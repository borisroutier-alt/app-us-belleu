name: Build APK Club
on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout du projet
        uses: actions/checkout@v3

      - name: 🟢 Configuration de Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: ☕ Configuration de Java JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      # AJOUT CONTOURNEMENT : On installe EAS CLI globalement avec npm avant l'action Expo
      - name: 🛠 Installation manuelle de EAS CLI
        run: npm install -g eas-cli

      - name: 🚀 Configuration de l'environnement Expo
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Dépendances du projet
        run: npm install

      - name: 🛠 Compilation de l'APK (Preview)
        run: eas build --platform android --profile preview --non-interactive